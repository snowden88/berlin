<div ng-controller="OrdersCtrl">
  <table class="table table-hover">
  <thead>
    <td>Order ID</td>
    <td>Total</td>
    <td>Product</td>
    <td></td>
  </thead>
  <tr ng-repeat="order in orders | orderBy:'-id':reverse">
    <td>
  {{order.id}}
</td>
<td>
  <strong>{{order.total | currency}}</strong>
</td>
<td>
  {{order.product_id}}
  <small ng-show="order.uder_id"><br>-{{order.user_id}}</small>
</td>
<td>
  <!-- we will fill this later with content -->
</td>
</tr>
</table>
</div>
